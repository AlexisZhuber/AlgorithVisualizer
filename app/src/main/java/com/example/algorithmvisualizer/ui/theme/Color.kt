package com.example.algorithmvisualizer.ui.theme

import androidx.compose.ui.graphics.Color

val Primary = Color(0xFF2196F3)    // Azul (para botones y acentos)
val Secondary = Color(0xFFFF5252)  // Rojo (para resaltar el nodo actual o la mejor ruta)
val Gray = Color(0xFFBDBDBD)         // Gris claro
val TextColor = Color(0xFF000000)    // Negro para textos
val DarkGray = Color(0xFF333333)     // Gris oscuro
val LightGray = Color(0xFFD3D3D3)    // Gris muy claro
val White = Color(0xFFFFFFFF)        // Blanco
val BackgroundCard = Color(0xFFCBDBEF)    // Azul (para botones y acentos)

// Colores adicionales para etiquetas y efectos:
val Yellow = Color(0xFFFFEB3B)       // Amarillo (para "glow" en el nodo actual)
val Green = Color(0xFF4CAF50)        // Verde (para el origen)
val Blue = Color(0xFF1976D2)         // Azul oscuro (para el destino)
val Red = Color(0xFFD3064B)
